extends ../../layouts/default

block head
  - var pageTitle = 'ProPlan для собак - официальный сайт'

block content
  +b.index
    +header()
    +screen(true)._carousel#carousel
      +carousel().__carousel
    +screen()._products#products
      +products().__products
